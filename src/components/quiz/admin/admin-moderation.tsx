"use client";

/**
 * Re-export the moderation panel so the admin/ folder is a complete collection
 * of all admin-tab components.
 *
 * The actual implementation lives in /components/quiz/moderation-panel.tsx
 * (created in task P8) — it doesn't need to be re-implemented here.
 */
export { ModerationPanel as AdminModerationPanel } from "@/components/quiz/moderation-panel";
export { ModerationPanel } from "@/components/quiz/moderation-panel";
